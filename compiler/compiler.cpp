#include "lexer.h"
#include "parser.h"

std::string ans;

exp test; 
std::stack<std::string> s; 

void search(exp test) {
    if(!(test.binExpr->lExp)) {

    }
}

int main() {
    //createtmp(test); 

    search(test); 
}